---
"lspee": patch
---

Add a minimal stub crate named `lspee` to reserve and preserve the package name while implementation crates remain split across `lspee_cli`, `lspee_daemon`, `lspee_lsp`, `lspee_config`, and `lspee_protocol`.
