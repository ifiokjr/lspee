---
"monokit": patch
---

Add a minimal stub crate named `monokit` to reserve and preserve the package name while implementation crates remain split across `monokit_cli`, `monokit_daemon`, `monokit_lsp`, `monokit_config`, and `monokit_protocol`.
